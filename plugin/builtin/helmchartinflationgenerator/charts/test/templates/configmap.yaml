apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "test.fullname" . }}
  labels:
    {{- include "test.labels" . | nindent 4 }}
data:
  key-with-yaml-document-end-marker: |
    #   +------------------+      +---------------+
    #   +------------------+      +---------------+
