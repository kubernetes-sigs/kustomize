apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Composition

transformersFrom:
- path: base
  importMode: prepend

transformers:
# These resources will not receive the label from the imported labeller
- metadata:
    name: prod-resources
  kind: ResourceGenerator
  files:
    - config_map.yaml
