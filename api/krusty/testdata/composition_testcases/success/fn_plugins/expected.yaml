apiVersion: v1
data:
  source: baseComposition
kind: ConfigMap
metadata:
  labels:
    labelled-by-base: "true"
    labelled-by-overlay: "true"
  name: myapp
---
apiVersion: v1
kind: Service
metadata:
  labels:
    labelled-by-base: "true"
    labelled-by-overlay: "true"
  name: myapp
spec:
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    app: myapp
