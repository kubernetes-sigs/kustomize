apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test-output-regex-replace
spec:
  rules:
  - host: replaced.example.com
    http:
      paths:
      - backend:
          serviceName: my-service
          servicePort: 80